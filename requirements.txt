# ── Core ML stack ──────────────────────────────────────────────────────────────
# Pin to a torch version that ships with numpy 2.x support.
# torch 2.2.0+ officially supports numpy 2.x on Python 3.8-3.11.
# Windows (CPU):  use the index below — it includes torchvision.
# If you have a GPU, replace "cpu" with your CUDA version, e.g. "cu121".
#
# Recommended install command (run this FIRST, before pip install -r):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# Then:
#   pip install -r requirements.txt

torch>=2.2.0
torchvision>=0.17.0
numpy>=1.24.0,<3.0.0
matplotlib>=3.7.0
pandas>=2.0.0
pyyaml>=6.0
scipy>=1.10.0
